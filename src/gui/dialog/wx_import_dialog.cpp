#include "wx_import_dialog.h"

wxImportDialog::wxImportDialog(wxWindow* parent)
    : wxBaseDialog(parent)
{

}

// TODO
void wxImportDialog::construct_preview_panel() {
    // Construct temp database for preview
    // preview_panel_ = new ViewPanel(this);
}